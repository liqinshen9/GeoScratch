# Supabase backend (optional). Without these the app runs untracked -- no auth,
# no attempt logging. Copy to .env.local and fill in from your Supabase project
# (Project Settings -> API). See docs/architecture/backend.md.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
